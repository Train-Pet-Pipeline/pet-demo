// Central constants — no hardcoded numbers in middleware / components / route handlers.
export const SESSION_TTL_DAYS = 7;
export const RATE_LIMIT_PER_MIN = 5;
export const COOKIE_NAME = "purrai_session";
export const ARTIFACTS_BASE = "/artifacts";
